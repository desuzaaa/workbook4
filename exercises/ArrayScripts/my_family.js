let contacts = [{ name: "John" }, { name: "Sam" }, { name: "Kyle" }, { name: "Laura" }];


for (let index = 0; index < contacts.length; index++) {
    const item = contacts[index];
    
    console.log(item.name);
    
}